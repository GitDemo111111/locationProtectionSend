# =============================================
# Python 编译缓存（必须忽略）
# 原因：Python 运行时会自动生成这些 .pyc 缓存文件来加速加载。
# 这些文件是二进制格式，跨平台不兼容，且毫无源码价值，提交到 Git 只会造成冲突和冗余。
# =============================================
__pycache__/
*.pyc
*.pyo
*.pyd

# Python 虚拟环境配置缓存（偶尔生成，忽略掉更干净）
.Python

# =============================================
# 虚拟环境目录（绝对必须忽略！）
# 原因：虚拟环境通常有几百兆，包含了大量特定于你操作系统（Windows）的二进制库文件。
# CI 服务器（如 GitHub Actions 使用的是 Linux 系统）无法使用这些文件。
# 如果把这个传上去，不仅会拖慢 Git 速度，还会导致 CI 报错。依赖包应该由 requirements.txt 来管理。
# =============================================
venv/
.venv/
env/
ENV/

# =============================================
# IDE 配置文件（建议忽略）
# 原因：.idea/ 是 PyCharm 的工程配置，.vscode/ 是 VS Code 的配置。
# 这里面包含了你个人的窗口布局、运行按钮的路径、本地调试设置。
# 每个人的电脑路径不同，传上去会互相覆盖，导致别人打不开项目。
# =============================================
.idea/
.vscode/

# =============================================
# Selenium 运行时产物（测试报告/截图/日志）
# 原因：这些是测试*执行后*产生的临时文件。
# *.log 是运行日志，screenshots/ 是失败时的截图。
# 这些文件每次运行都会变化，而且体积会不断累积。
# 它们应该由 CI 的 "Upload Artifacts" 功能在云端保存，而不是被提交到 Git 仓库中。
# =============================================
*.log
screenshots/